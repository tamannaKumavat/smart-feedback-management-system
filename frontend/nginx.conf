server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        try_files $uri $uri/ /index.html;
    }

    location /api/ {
        proxy_pass https://sfms-backend.27cm6ddk8tsg.eu-de.codeengine.appdomain.cloud/api/;
        proxy_set_header Host sfms-backend.27cm6ddk8tsg.eu-de.codeengine.appdomain.cloud;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /ws/ {
        proxy_pass https://sfms-backend.27cm6ddk8tsg.eu-de.codeengine.appdomain.cloud/ws/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host sfms-backend.27cm6ddk8tsg.eu-de.codeengine.appdomain.cloud;
    }
}
